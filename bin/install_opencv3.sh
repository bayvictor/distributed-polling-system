 sudo apt-get install -y --force-yes libopencv3.1-java #- Java bindings for the computer vision library
 sudo apt-get install -y --force-yes libopencv3.1-jni #- Java jni library for the computer vision library
 sudo apt-get install -y --force-yes ros-indigo-opencv3 #- OpenCV 3.0
 sudo apt-get install -y --force-yes ros-jade-opencv3 #- OpenCV 3.x


 sudo apt-get install -y --force-yes libcv-dev #- Translation package for libcv-dev
 sudo apt-get install -y --force-yes libcvaux-dev #- Translation package for libcvaux-dev
 sudo apt-get install -y --force-yes libhighgui-dev #- Translation package for libhighgui-dev
 sudo apt-get install -y --force-yes libopencv-contrib-dev #- development files for libopencv-contrib
 sudo apt-get install -y --force-yes libopencv-dev #- development files for opencv
 sudo apt-get install -y --force-yes libopencv-gpu-dev #- development files for libopencv-gpu2.4
 sudo apt-get install -y --force-yes libopencv-legacy-dev #- development files for libopencv-legacy
 sudo apt-get install -y --force-yes libopencv-ocl-dev #- development files for libopencv-ocl2.4
 sudo apt-get install -y --force-yes opencv-data #- development data for opencv
 sudo apt-get install -y --force-yes libopencv-calib3d-dev #- development files for libopencv-calib3d3.1
 sudo apt-get install -y --force-yes libopencv-core-dev #- development files for libopencv-core3.1
 sudo apt-get install -y --force-yes libopencv-features2d-dev #- development files for libopencv-features2d3.1
 sudo apt-get install -y --force-yes libopencv-flann-dev #- development files for libopencv-flann3.1
 sudo apt-get install -y --force-yes libopencv-highgui-dev #- development files for libopencv-highgui3.1
 sudo apt-get install -y --force-yes libopencv-imgcodecs-dev #- development files for libopencv-imgcodecs3.1
 sudo apt-get install -y --force-yes libopencv-imgproc-dev #- development files for libopencv-imgproc3.1
 sudo apt-get install -y --force-yes libopencv-ml-dev #- development files for libopencv-ml3.1
 sudo apt-get install -y --force-yes libopencv-objdetect-dev #- development files for libopencv-objdetect3.1
 sudo apt-get install -y --force-yes libopencv-photo-dev #- development files for libopencv-photo3.1
 sudo apt-get install -y --force-yes libopencv-shape-dev #- development files for libopencv-shape3.1
 sudo apt-get install -y --force-yes libopencv-stitching-dev #- development files for libopencv-stitching3.1
 sudo apt-get install -y --force-yes libopencv-superres-dev #- development files for libopencv-superres3.1
 sudo apt-get install -y --force-yes libopencv-ts-dev #- development files for TS library of OpenCV (Open Computer Vision)
 sudo apt-get install -y --force-yes libopencv-videoio-dev #- development files for libopencv-videoio3.1
 sudo apt-get install -y --force-yes libopencv-videostab-dev #- development files for libopencv-videostab3.1
 sudo apt-get install -y --force-yes libopencv-video-dev #- development files for libopencv-video3.1
 sudo apt-get install -y --force-yes libopencv-viz-dev #- development files for libopencv-viz3.1
 sudo apt-get install -y --force-yes ros-indigo-video-stream-opencv #- The video_stream_opencv package contains a node to publish a video stream (the protocols that opencv supports are supported, including rtsp, webcams on /dev/video and video files) in ROS image topics, it supports camera info and basic image flipping (horizontal, vertical or both) capabilities.

 sudo apt-get install -y --force-yes ros-indigo-anj-featurenav #- The anj_featurenav package provides a learning jockey and a navigating jockey for the Large Maps framework (LaMa). It learns a path by saving image features and is able to follow the same path. It is based on algorithms provided by OpenCV (free ones).
 sudo apt-get install -y --force-yes ros-indigo-camera-pose-calibration #- Camera pose calibration using the OpenCV asymmetric circles pattern.
 sudo apt-get install -y --force-yes ros-indigo-checkerboard-detector #- Uses opencv to find checkboards and compute their 6D poses with respect to the image.
 sudo apt-get install -y --force-yes ros-indigo-cost-map-cv #- Conversions between cost_maps and opencv images.
 sudo apt-get install -y --force-yes ros-indigo-cv-backports #- Backporting features from the 2.4.x.y series of opencv releases to indigo.
 sudo apt-get install -y --force-yes ros-indigo-cv-bridge #- This contains CvBridge, which converts between ROS Image messages and OpenCV images.
 sudo apt-get install -y --force-yes ros-indigo-cv-camera #- cv_camera uses OpenCV capture object to capture camera image. This supports camera_image and nodelet.
 sudo apt-get install -y --force-yes ros-indigo-ecto-opencv #- Ecto bindings for common opencv functionality.
 sudo apt-get install -y --force-yes ros-indigo-grid-map-cv #- Conversions between grid maps and OpenCV images.
 sudo apt-get install -y --force-yes ros-indigo-gridmap-2d #- gridmap_2d #- a simple 2D gridmap structure, based on OpenCV's cv::Mat
 sudo apt-get install -y --force-yes ros-indigo-image-geometry #- `image_geometry` contains C++ and Python libraries for interpreting images geometrically.
 sudo apt-get install -y --force-yes ros-indigo-object-recognition-linemod #- An object recognition pipeline that uses LINE-MOD from OpenCV
 sudo apt-get install -y --force-yes ros-indigo-opencv-apps #- opencv_apps provides various nodes that run internally OpenCV's functionalities and publish the result as ROS topics.
 sudo apt-get install -y --force-yes ros-indigo-opencv-candidate #- Experimental OpenCV functions
 sudo apt-get install -y --force-yes ros-indigo-opencv3 #- OpenCV 3.0
 sudo apt-get install -y --force-yes ros-indigo-qglv-opencv #- Qt, QGLViewer and Opencv.
 sudo apt-get install -y --force-yes ros-indigo-slic #- SLIC-Superpizel ROS Wrapper This file contains the class elements of the class Slic.
 sudo apt-get install -y --force-yes ros-indigo-swri-opencv-util #- swri_opencv_util
 sudo apt-get install -y --force-yes ros-indigo-video-stream-opencv #- The video_stream_opencv package contains a node to publish a video stream (the protocols that opencv supports are supported, including rtsp, webcams on /dev/video and video files) in ROS image topics, it supports camera info and basic image flipping (horizontal, vertical or both) capabilities.
 sudo apt-get install -y --force-yes ros-indigo-vision-opencv #- Packages for interfacing ROS with OpenCV, a library of programming functions for real time computer vision.
 sudo apt-get install -y --force-yes ros-jade-checkerboard-detector #- Uses opencv to find checkboards and compute their 6D poses with respect to the image.
 sudo apt-get install -y --force-yes ros-jade-cv-backports #- Backporting features from the 2.4.x.y series of opencv releases to indigo.
 sudo apt-get install -y --force-yes ros-jade-cv-bridge #- This contains CvBridge, which converts between ROS Image messages and OpenCV images.
 sudo apt-get install -y --force-yes ros-jade-cv-camera #- cv_camera uses OpenCV capture object to capture camera image. This supports camera_image and nodelet.
 sudo apt-get install -y --force-yes ros-jade-ecto-opencv #- Ecto bindings for common opencv functionality.
 sudo apt-get install -y --force-yes ros-jade-grid-map-cv #- Conversions between grid maps and OpenCV images.
 sudo apt-get install -y --force-yes ros-jade-image-geometry #- `image_geometry` contains C++ and Python libraries for interpreting images geometrically.
 sudo apt-get install -y --force-yes ros-jade-object-recognition-linemod #- An object recognition pipeline that uses LINE-MOD from OpenCV
 sudo apt-get install -y --force-yes ros-jade-opencv-apps #- opencv_apps provides various nodes that run internally OpenCV's functionalities and publish the result as ROS topics.
 sudo apt-get install -y --force-yes ros-jade-opencv-candidate #- Experimental OpenCV functions
 sudo apt-get install -y --force-yes ros-jade-opencv3 #- OpenCV 3.x
 sudo apt-get install -y --force-yes ros-jade-slic #- SLIC-Superpizel ROS Wrapper This file contains the class elements of the class Slic.
 sudo apt-get install -y --force-yes ros-jade-swri-opencv-util #- swri_opencv_util
 sudo apt-get install -y --force-yes ros-jade-vision-opencv #- Packages for interfacing ROS with OpenCV, a library of programming functions for real time computer vision.



