# some software
## evoluspencil: a mockup tool
## metapost: a powerful tool for creating graphics
## scilab: Matlab-like ## maxima: Computer Algebra System # file management apt-get install vifm
# vim-like pdf viewer, an alternative is Zathura
apt-get install apvlv
# browser
apt-get install elinks
apt-get install w3m
# picture viewer
apt-get install zgv
apt-get install feh
# email
apt-get install mutt
# dropdown terminal
apt-get install guark
# disk manage and view
apt-get install gparted
# screen saver
# sudo apt-get remove gnome-screensaver
# sudo apt-get install xscreensaver xscreensaver-data-extra xscreensaver-gl-extra
# keyboard short cuts: xscreensaver-command -lock
apt-get install xscreensaver
# languages
apt-get install lua5.2
# dynamic menu
apt-get install dmenu
# awesome extra vicious
apt-get install awesome-extra
# haskell platform
apt-get install haskell-platform
# zsh
apt-get install zsh
# teminator; this can split window
apt-get install teminator
# xbindkey xbindkey -k -mk xbindkeys --default > ~/.xbindkeysrc
apt-get install xbindkeys
# rxvt-unicode
apt-get install rxvt-unicode
# another terminal
apt-get install mlterm
# xchat
apt-get install xchat
# openbox
#apt-get install openbox
# xingjiyiwang
#  sudo mv stardict-langdao-ce-gb-2.4.2 /usr/share/stardict/dic/  ~/.stardict/dic
apt-get install stardict
# picture process
apt-get install imagemagick
# dia: visio-like
apt-get install dia
# gnupg: encription tool commandline=>gpg
apt-get install gnupg
# texlive-full(texlive) and seems texlive-bath: latex
# 
apt-get install texlive
apt-get install latex-cjk-all 
apt-get install texmaker
# WYSIWYM WYSIWYG
apt-get install lyx
# latex-beamer – Beamer package is used to create presentations. It is an excellent LaTeX class that supports dynamic effects.
# TeXPower – Is a bundle of style and class files for creating dynamic online presentations with LaTeX.
# Prosper – A LaTeX class for writing transparencies.
# texlive-pictures – This is a LaTeX package for drawing graphics. It contain several classes such as ‘curve’ (for creating resumes), ‘bardiag’ (for bar graphs), ‘pmgraph’ (poor man’s graphics) and so on.
#texlive-latex-extra – This is a large collection of addon packages for LaTeX. The full list of classes in this package are listed here.
# This is the full command I used to install LaTeX on my machine running Ubuntu Linux.
# apt-get install gedit-latex-plugin texlive-fonts-recommended latex-beamer texpower texlive-pictures texlive-latex-extra texpower-examples 
#apt-get install gedit-latex-plugin
# xdvi - DVI Previewer for the X Window System, it's installed by texlive, xdvi *.dvi, can used for presentation
#apt-get install xdvi

# R
apt-get install r-base
# sougou pinyin# mv sudo mv stardict-langdao-ce-gb-2.4.2 /usr/share/stardict/dic/
# http://abloz.com/huzheng/stardict-dic/zh_CN/
# fcitx-config-gtk
#  fcitx-configtool
sudo add-apt-repository ppa:fcitx-team/nightly
sudo apt-get install fcitx-sogoupinyin
# install emacs
sudo add-apt-repository ppa:cassou/emacs
apt-get install emacs24 emacs24-el emacs24-common-non-dfsg

# conky monitor
apt-get install conky
